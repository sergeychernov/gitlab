{
    "description": "Trunk-based: a single main + short-lived branches, releases via CD",
    "groups": [
        { "label": "main",                 "ref": "main" },
        { "label": "short-lived branches", "scope": "branches", "excludeRef": "main" }
    ]
}
